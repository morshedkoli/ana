# Optional environment overrides
# These are also configurable in the Settings page (preferred for credentials)

# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_API_TOKEN=

# Set to production when deployed
NODE_ENV=development
